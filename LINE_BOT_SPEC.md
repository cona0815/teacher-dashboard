# LINE 小幫手整合規格書 v1.0（定稿）

狀態：**規格已定稿，尚未實作；等使用者說「開工」才動程式**
定稿日期：2026-08-25

## 0. 使用者已拍板的決策紀錄

| 決策 | 結論 |
| --- | --- |
| 桌寵同步方式 | 方案 A：桌寵維持零網路，經網頁 127.0.0.1 橋接取得 LINE 資料 |
| 第一版功能範圍 | 全做：①傳任務 ②傳記事 ③查今日進度／任務 ④聯絡簿公告 ⑤晨間推播 |
| 系統→老師的訊息 | 兩種都做：您先問 bot 即回（免費不限量）＋每日晨間定時推播（月額度內） |
| AI 依賴 | 規則模式退場機制：沒 Gemini 金鑰用「任務：」「記：」等前綴，功能不中斷 |
| 同步時機 | 開網頁自動同步一次＋「立即同步」手動按鈕 |
| 收件匣位置 | 教師工具箱新增獨立按鈕，角標顯示待處理數量 |
| 聯絡簿公告 | LINE 即時回覆排版好的公告，老師長按轉發家長群 |
| v1 加量（參考 ijun-ai 文章後追加） | 全做：撤回／刪除指令、記事細分類標籤、語音直錄、拍照辨識 |
適用專案：教師工作台（localStorage 本機優先）＋桌面小綿助

---

## 1. 目標與不可違反的原則

1. **localStorage 是唯一主資料庫**。LINE／GAS／試算表只是「雲端轉運信箱」，訊息被工作台收下後正本就在瀏覽器。
2. **完全選用**：不安裝 LINE 小幫手時，網頁與桌面小綿助的所有既有功能一概不受影響。
3. **每位老師自有雲端**：自己的 GAS 部署＋自己的試算表＋自己的 LINE 官方帳號。沒有中央伺服器，開發者收不到任何老師的資料。
4. **桌面小綿助維持零網路**（已確認採方案 A）：桌寵只監聽 127.0.0.1，LINE 資料經網頁橋接轉入；不推翻既有安全契約與測試。
5. 金鑰（LINE Token、Gemini 金鑰、同步金鑰）不進 Git、不進 JSON 備份、不進交付 ZIP。
6. 所有 LINE 進來的內容都要**經老師在工作台確認**才正式成為任務／記事（收件匣模式）。

## 2. 系統組成與資料流

```
老師手機 LINE
   │  傳訊息
   ▼
LINE 官方帳號（老師自己申請，免費方案）
   │  Webhook（網址帶隨機驗證參數）
   ▼
GAS「LineBot.gs」（老師自己部署）
   │  分類：Gemini（選用）或前綴規則
   ▼
Google 試算表（兩張表：LINE收件匣、工作台快照）
   ▲│
   │▼  網頁同步（拉收件匣＋推快照，帶同步金鑰）
教師工作台網頁（localStorage 主資料庫）
   ▲│
   │▼  既有 127.0.0.1:8767 本機橋接（擴充欄位）
桌面小綿助（零網路，維持現狀）
```

## 3. LINE 端功能規格（老師手機看到的行為）

### 3.1 訊息分類（五種）

| 類型 | 觸發方式 | Bot 回應 |
| --- | --- | --- |
| 任務 task | 自然語句（AI 判斷）或「任務：」前綴 | 「已收到，整理成任務放進 LINE 收件匣」＋標題／到期日／主軸 |
| 記事 note | AI 判斷（學生狀況、家長聯繫、心得）或「記：」前綴 | 「已收到，整理成記事」確認 |
| 查詢 query | 「今天上到哪」「今日任務」「今日進度」等 | 回覆工作台快照：今日各課程進度＋今日任務＋逾期數＋快照時間 |
| 聯絡簿 contact | 「聯絡簿：項目1、項目2」 | 立即回覆排版好的家長群公告文字（含日期星期、編號條列、結尾感謝語），老師長按轉發＋「轉發前確認無學生個資」提醒 |
| 說明 help | 「說明」「幫助」「help」 | 使用說明 |
| 查ID | 「我的ID」 | 回覆老師本人的 LINE userId（安裝設定 LINE_ALLOWED_USER_IDS 用） |
| 撤回 undo | 「撤回」 | 把該使用者最近一筆未處理的信箱項目作廢，回覆確認 |
| 刪除 delete | 「刪除 關鍵字」 | 搜尋未處理項目中含關鍵字的最新一筆並作廢，回覆確認 |

### 3.1.1 記事細分類標籤（v1 追加）

- Gemini 分類記事時同時給標籤：家長聯繫／學生概況／作業缺交／備課靈感／生活雜事（或無標籤）。
- 標籤存在信箱「補充JSON」欄；工作台收件匣與轉入後的記事都顯示【標籤】前綴。
- 規則模式（無 Gemini）不出標籤，功能不中斷。

### 3.1.2 語音直錄與拍照辨識（v1 追加）

- 語音訊息：GAS 以 LINE Content API 取回音檔 → 交給 Gemini 聽寫＋分類，流程與文字相同。
- 照片訊息：取回圖片 → Gemini 視覺萃取文字與重點（通知單、黑板、公文）→ 整理成任務／記事草稿進信箱。
- 兩者都**需要 Gemini 金鑰**；未設定金鑰時回覆「請先在指令碼屬性設定 GEMINI_API_KEY 才能使用語音／照片」。
- 大小上限：依 LINE 與 Gemini inline 限制，音檔／圖片超過約 15 MB 時回覆無法處理。

### 3.2 AI 依賴（已確認：規則模式退場機制）

- 有 Gemini 金鑰：自然語言自由輸入，AI 萃取標題、到期日（含「明天」「下週五」換算）、工作主軸（教學／行政／學年主任／導師）。
- 沒有金鑰：以「任務：」「記：」「聯絡簿：」前綴與關鍵字判斷；相對日期支援今天／明天／後天／「5/20」格式。功能不中斷。
- 無法判斷時保守預設為「任務草稿」——反正一定要經老師確認，錯了在收件匣改就好。

### 3.3 晨間簡報（已確認要做）

- GAS 時間觸發器（老師安裝時自行設定，建議每日 07:00–08:00）。
- 推播內容＝「☀️ 早安！」＋快照回覆（同查詢功能）。
- 用量：推播計入 LINE 免費額度（目前每月 200 則）；每日 1 則約 30 則／月，安全。
- 回覆訊息（老師先傳、Bot 回）完全免費、不限量。

### 3.4 安全

- Webhook 網址帶隨機 `?hook=<亂數>` 驗證（GAS 收不到 LINE 簽章標頭的補償措施）。
- `LINE_ALLOWED_USER_IDS`：鎖定只回應老師本人的 LINE userId；陌生人加好友傳訊息一律不理、不寫入。
- 第一版只收文字訊息；語音、照片回覆「之後版本加入」。

## 4. GAS 端規格（LineBot.gs，獨立新檔）

- **不改動既有 `Code.gs`**；LineBot.gs 是獨立檔案，可與 Code.gs 放同一 GAS 專案，也可獨立部署。
- 試算表兩張工作表：
  - `LINE收件匣`：編號、建立時間、類型、原文、整理標題、到期日、工作主軸、狀態（new／imported／ignored）、來源使用者、補充JSON。上限 1000 列，超過時自動清最舊的已處理列。
  - `工作台快照`：更新時間＋快照 JSON（單格）。
- 同步 API（POST JSON，帶同步金鑰）：
  - `ping`：測試連線
  - `pull`：取回狀態 new 的收件匣項目（上限 200 筆）
  - `ack`：把指定編號標成 imported／ignored
  - `snapshot`：儲存工作台推上來的快照
- Script Properties 保存：LINE_CHANNEL_ACCESS_TOKEN、LINE_WEBHOOK_TOKEN、LINE_SYNC_TOKEN、LINE_ALLOWED_USER_IDS、GEMINI_API_KEY（選填）、GEMINI_MODEL（選填）。
- `setupLineBot()`：一鍵初始化（建表＋產生兩把亂數金鑰＋輸出安裝摘要）。

## 5. 網頁工作台規格（Index.html）

### 5.1 系統設定 → 新分頁「LINE 小幫手」

- 欄位：啟用開關、GAS /exec 網址、同步金鑰（password 欄位）、「記住在這台電腦」勾選（比照 Gemini 金鑰：不勾則只存到分頁關閉）。
- 按鈕：「測試連線」（打 ping）、「立即同步」、連線狀態文字。
- 安裝教學卡：逐步說明＋`LineBot.gs` 完整程式碼下載／預覽（比照現有 GAS 安裝卡樣式）＋連到 `LINE_BOT_INSTALL.md`。
- 額度與隱私提示：免費額度說明、金鑰不進備份、建議鎖定本人 userId。

### 5.2 LINE 收件匣（新 UI 區塊）

- 每個項目顯示：LINE 圖示、類型（任務草稿／記事草稿）、整理標題、原文、到期日、建議主軸、收到時間。
- 操作：
  - **轉為任務**：可先改主軸（教學／行政／學年主任／導師）、細項分類、到期日，按下後走既有 `saveTask` 流程建立，任務備註標明「由 LINE 小幫手建立」。
  - **轉為記事**：進既有小綿助記事（來源標示 LINE）。
  - **忽略**：不建立。
- 收下或忽略後回報 GAS `ack`，收件匣不會重複拉到同一筆。
- 未處理項目數量在收件匣入口以角標顯示。

### 5.3 同步時機與快照

- 同步動作＝ `pull`（拉收件匣）＋ `snapshot`（推快照）一次完成。
- 快照內容（**只含摘要，不含學生個資與附件**）：
  ```json
  {
    "generatedAt": "…",
    "todayTasks": [{ "name": "…", "dueTime": "…", "axis": "…" }],
    "overdueCount": 3,
    "todayCourses": [{ "subject": "六年一班社會", "content": "第三單元 第2節" }]
  }
  ```
  今日課程取自各課程獨立排程中「今天有排」的列（教學／複習／評量），沿用既有 `getAllTeachingProgressRows()` 資料。
- CSP 白名單加開 `https://script.google.com` 與 `https://script.googleusercontent.com`（僅 connect-src）。

### 5.4 向下相容

- 新增 localStorage key：`grade6LineBridgeSettings`（啟用、網址、自動同步）、`grade6LineSyncToken`（金鑰，依記住選項存 local 或 session）、`grade6LineInbox`（收件匣暫存）。
- 不改動任何既有 key 的格式；未設定 LINE 時所有新程式不執行。
- JSON 備份**不包含**同步金鑰與 GAS 網址；收件匣暫存也不進備份（雲端信箱本身就是暫存的來源）。

## 6. 桌面小綿助規格（方案 A：零網路）

- 網頁在既有 `/sync` 橋接 payload 中新增 `lineInbox` 欄位（未處理項目的精簡清單：標題、類型、時間，上限 50 筆）。
- 桌寵把它存進 `secretary_data.json`（新欄位 `line_inbox`，向下相容：舊版檔案沒有此欄位也正常）。
- 秘書面板新增「LINE 待整理」清單卡（唯讀，最多顯示 5 筆＋總數）；今日簡報加一句「有 N 則 LINE 訊息待整理」。
- 桌寵**不做**匯入／確認動作（避免與網頁重複建立）；確認一律在工作台網頁。
- 已知限制（已確認接受）：瀏覽器沒開時，桌寵不會收到新 LINE 訊息。
- 不新增任何對外網路程式碼；既有測試契約（禁止 urlopen、禁止 GAS 同步欄位）維持通過。

## 7. 交付項目清單

| 項目 | 檔案 | 動作 |
| --- | --- | --- |
| GAS 後端 | `LineBot.gs` | 新增（草稿已寫好，規格核定後依定稿調整） |
| 網頁整合 | `Index.html` | 修改（設定分頁＋收件匣＋同步模組＋CSP） |
| 桌寵同步 | `desktop_pet_secretary.py` | 修改（橋接欄位＋顯示卡） |
| 安裝文件 | `LINE_BOT_INSTALL.md` | 新增（手把手：申請官方帳號→部署 GAS→設定 Webhook→工作台連線） |
| 測試 | `tests/profile_config.test.js`、`tests/local_bridge_test.py` | 擴充新契約 |
| 說明 | `README.md` | 補一段選用功能說明 |

## 8. 驗收清單（實作完成後逐項測）

1. 未設定 LINE 時：網頁、桌寵、備份還原全部行為與現在完全相同。
2. 設定錯誤金鑰：同步顯示明確錯誤，不影響其他功能。
3. LINE 傳任務→收件匣出現→轉為任務→出現在清單與日曆→GAS 信箱標記 imported→再同步不重複。
4. 轉為記事、忽略流程同上。
5. 沒有 Gemini 金鑰時，前綴規則五種訊息都正確回應。
6. LINE 查詢在（a）未推快照（b）已推快照 兩種情況回覆正確。
7. 聯絡簿公告格式正確、可直接轉發。
8. 桌寵開啟時：網頁同步後桌寵出現「LINE 待整理」；桌寵關閉時網頁一切正常。
9. JSON 備份檔內找不到 GAS 網址與任何金鑰；還原後 LINE 設定不被覆蓋。
10. `npm.cmd test`、`python -m py_compile`、既有手動驗收清單全數通過。

## 9. 第一版不做（列入後續版本候選）

- Flex Message 精美卡片回覆（第一版用純文字＋emoji）
- LINE 端直接「確認建立」（繞過收件匣）
- 記帳本、好物分享（偏離教師工作台定位，確認不做）
- 行事曆同步 Google Calendar（工作台已有日曆，重複性高）
- 多位老師共用一個 Bot

（原列於此的語音辨識、照片辨識、家長聯繫等細分類，已於 2026-08-25 經使用者確認移入 v1，見 3.1.1、3.1.2。）

## 模組 B：晨間大屏（點名＋作業繳交）——已核定，於 LINE v1 完成後實作

使用者 2026-08-25 拍板：

| 決策 | 結論 |
| --- | --- |
| 參考舊系統 | 不用，照新設計做 |
| 作業登記 | 點「已交」的座號（逐一確認收到才點） |
| 出缺類別 | 四種：病假／事假／遲到／未到（點按循環切換），預設全到 |
| 實作順序 | 先完成 LINE v1 收尾驗收，再做大屏模組 |

### B.1 組成

- `Morning.html`：教室大屏觸控頁（單檔、Netlify 部署、只用座號不用姓名）。
  - 點名區：座號按鈕矩陣（人數、班名可設定，存 localStorage），點按循環：到→病假→事假→遲到→未到→到。
  - 作業區：選科目（清單可設定）→ 點「已交」座號 → 未點者即缺交。
  - 「送出」上傳 GAS；當日可重送覆蓋（以日期＋班級為鍵）。
  - **互動設計參考「作業訂正小幫手」影片（鄭念慈，使用者 2026-08-25 指定參考）**：
    - 視覺化座號方塊配色：**綠＝已交、紅＝缺交、藍＝補交**，大螢幕投影一眼看懂全班狀況。
    - **收件／訂正雙模式**：收件模式登記已交；訂正模式列出需訂正名單，學生補交或訂正完成時「點一下過關」自動變藍。
    - **雙欄位並排**：同一畫面左右並排兩種作業狀態（例：清點欄＋訂正欄），不必切換頁面。
    - **全班缺交總表／訂正總表**：跨科一張表看全班進度（連動 B.1 查詢統計）。
    - **大電子鐘＋倒數計時**：晨間常規節奏管理，全班限時挑戰。
    - 作業狀態因此為三種：已交／缺交／補交（缺交→補交連動自動更新，統計分開計）。
- GAS 擴充（與 LineBot.gs 同專案）：
  - 新增工作表：`點名紀錄`、`作業紀錄`。
  - 新 API 動作（用**獨立教室金鑰** CLASSROOM_TOKEN，權限僅限寫當日＋查當日；老師金鑰才能查歷史）。
  - 送出後即時推播老師 LINE：缺席與各科缺交摘要。
  - LINE 指令：「今日點名」「缺交統計」。
- 查詢介面：`Morning.html` 內建查詢分頁（輸入老師同步金鑰後解鎖）：
  - 單日明細：某天的出缺席與各科繳交名單。
  - **分科統計**：每一科獨立統計，不混在一起（某生「數學」缺交幾天、「國語」缺交幾天分開看）。
  - **天數累計**：可選日期區間（本週／本月／學期／自訂起訖），計算每位座號在區間內的缺席天數（分病假／事假／遲到／未到）與各科缺交天數。
  - 科目繳交率：某科在區間內的全班平均繳交率。
  - LINE 查詢同樣支援：「缺交統計」回覆各科分開的累計天數排行（只列有缺交者）。

### B.2 安全

- 教室金鑰與老師同步金鑰分離；大屏頁面不儲存老師金鑰。
- 只傳座號與狀態，無姓名、無成績。

### B.3 作業統計與工作台日曆整合（使用者 2026-08-25 追加）

- 工作台同步時一併拉回大屏統計摘要，**顯示在工作台日曆**：每個上課日的格子顯示當日出缺與缺交摘要（例：「缺2・缺交3」），點開日曆卡片可看座號明細。
- 統計維度：分科、分日、區間天數累計（同 B.1 查詢介面）。
- 資料流：大屏 → GAS 統計表 → 工作台同步拉回（進 localStorage）→ 日曆渲染；LINE 也可查「今日點名」「缺交統計」。

### B.4 與既有「智慧作業檢核系統」的關係（待使用者提供資料）

- 使用者提供了部署於學校網域的舊系統網址（智慧作業檢核系統，go.edu.tw GAS）。
- 該系統需學校帳號登入，開發者無法直接讀取其介面與程式碼。
- 待使用者提供該系統的 Code.gs／HTML 原始檔後決定：沿用其大屏操作介面習慣改造，或依本規格全新實作、再把舊資料匯入。

## 模組 B v2：大屏第二版需求（使用者 2026-08-25 晚間實測後追加）

### B-v2.1 設定強化

- **空號**：設定可填空號清單（例：4、13），這些座號不出現在任何格子與統計。
- **科目管理**：內建國小常見科目（國語、數學、英語、自然、社會、生活、健康與體育、音樂、視覺藝術、綜合、本土語）＋「通知單」；可自訂新增；可勾選哪些科目出現在作業繳交。
- **作業名稱**：可自訂下拉選單（預設：習作、試卷、作業本、學習單、其他）。
- **時間自動化**（皆可自訂，預設如下）：
  - `08:00` 遲到界線：之後才點「到校」→ 大屏顯示「到」，**資料記為遲到**（遲到只有老師在 LINE／查詢看得到，大屏永不顯示，保護學生）。
  - `08:05` 自動缺席：仍空白（未填）的座號自動轉「未到」。
  - `08:08` 自動回報：自動送出並推播 LINE 給老師今日到校情況。

### B-v2.2 點名 v2

- **預設全部空白（未填）**，不再預設到校。
- 可見狀態循環：空白→到→病假→事假→未到→空白（**遲到不在大屏循環中**，只由時間規則隱性記錄）。
- 8:08 之後狀態有變（如未到的學生到了）→ 老師可再按送出更新，LINE 收到更新版。

### B-v2.3 作業繳交 v2

- 收作業的單位從「科目」變成「**版面**」＝科目＋作業名稱（例：國語／習作、數學／試卷）。
- **今日最多同時 6 個版面**；新增版面時選科目＋作業名稱。
- 頁面最下方顯示**今日統計（分科分版面）**：每個版面的已交／補交／缺交。
- 試算表「作業紀錄」增加「作業名稱」欄（自動遷移舊表頭）。

### B-v2.4 新分頁「作業補交」

追蹤**過去 30 天**所有缺交（含跨日），三種顯示模式：

1. **按科目**：選科目 → 列出有缺交的座號與筆數 → 點座號 → 顯示是哪一天、哪個作業沒交。
2. **依座號**：只顯示仍有缺交的座號 → 點座號 → 該生所有未交明細。
3. **日曆**：月曆格顯示每天的缺交筆數 → 點日期看明細。

每筆明細都有「**完成**」按鈕 → 該筆從缺交改為**補交**（歷史紀錄保留缺交事實＋補交標記，統計分開計）。

### B-v2.4.1 版面與自動化的落地決策（使用者核定）

- 六個作業版面採**同一畫面並排**（響應式格狀，各版面有自己的座號小格與統計）。
- 8:05／8:08 自動化由**大屏頁前端執行**（頁面開著才動作）；另提供 GAS `sendRollcallReminder()` 供老師設 8:10 雲端定時**備援**：當天還沒有任何回報時，LINE 提醒「今日尚未點名」。
- 遲到界線、自動缺席、自動回報三個時間與補交回溯天數（預設 08:00／08:05／08:08／30 天）都在設定頁可自訂。

### B-v2.6 聯絡本黑板（使用者 2026-08-26 追加，含參考圖）

- 大屏「點名」畫面右側新增**黑板風聯絡本**（木框、墨綠底、粉筆字、右上角日期，仿「班級魔法版」參考圖）。
- 大屏可直接「✏️ 編輯」手動輸入；開頁自動載入今日內容，可手動重新整理。
- 教師工作台新增「聯絡本」管理版面（教師工具箱按鈕，啟用 LINE 小幫手後顯示）：日期選擇器＋讀取＋儲存，可**預先輸入任何日期**的聯絡本。
- 同步規則：依日期一筆記錄存於試算表「聯絡本」工作表；**同一天以最後儲存者為準**；老師通常前一晚在工作台備好，早上大屏開頁即顯示。
- 權限：教室金鑰只能讀寫「今天」；老師金鑰可讀寫任何日期。

### B-v2.7 每週作業關懷（使用者 2026-08-26 核定）

- GAS `sendWeeklyHomeworkAlerts()`（建議每週五下午時間觸發器）：統計近 7 天缺交次數，達門檻（預設 5 次，指令碼屬性 `WEEKLY_ALERT_THRESHOLD` 可調）者，自動產生**委婉家長訊息草稿**（三明治結構、留「孩子優點」空格由老師補填）放進 LINE 收件匣（標籤【家長聯繫】），並 LINE 推播達標座號清單給老師。補交過的不計入。

### B-v2.5 權限調整

教室金鑰新增兩個受限動作：讀取近 30 天缺交清單（只有座號無其他資料）、把單筆缺交標為補交。其餘歷史查詢仍需老師金鑰。

## 10. 開放問題

（無。原本的三個開放問題已由使用者於 2026-08-25 拍板，見第 0 節決策紀錄。）

### 聯絡簿公告的補充說明

聯絡簿採 LINE 即時回覆：因為公告內容是老師自己輸入的作業與注意事項（非學生個資），
去識別化風險低；但安裝文件與 bot 回覆中仍要提醒「轉發前請自行確認內容沒有學生姓名等個資」。
