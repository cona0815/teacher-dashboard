# LINE 小幫手安裝說明（選用功能）

LINE 小幫手讓你在 LINE 隨手傳文字、語音或照片給自己的機器人，訊息會出現在教師工作台的「LINE 收件匣」，由你確認後建立任務或記事；也能在 LINE 查詢今日課程進度、產生聯絡簿公告、每天早上收到簡報。

- 完全選用：不安裝時，教師工作台與桌面小綿助一切照舊。
- 你的資料留在你自己的 Google 帳號與 LINE 官方帳號，沒有任何中央伺服器。
- 免費：LINE 官方帳號免費方案＋Google Apps Script 免費額度即可運作。

安裝約需 30–60 分鐘，只需做一次。過程需要：Google 帳號、LINE 帳號、一支手機。

---

## 第 1 步：建立 LINE 官方帳號與 Messaging API

1. 前往 [LINE Developers Console](https://developers.line.biz/console/)，用你的 LINE 帳號登入。
2. 建立 Provider（名稱自訂，例如「我的教師工具」）。
3. 建立 **Messaging API** channel：
   - 也可先到 [LINE 官方帳號管理頁](https://manager.line.biz/) 建立官方帳號，再於「設定 → Messaging API」啟用。
   - Channel 名稱例如「班級小幫手」。
4. 進入 channel 的 **Messaging API** 分頁：
   - 記下（稍後要用）：**Channel access token**（按 Issue 產生）。
   - 「LINE Official Account features」中：**停用**「自動回覆訊息」與「加入好友的歡迎訊息」（改由程式回覆）。
5. 用手機掃描該分頁的 QR Code，把自己的機器人加為好友。

## 第 2 步：建立試算表並部署 Apps Script

1. 到 [Google 試算表](https://sheets.google.com) 建立新試算表，名稱例如「LINE 小幫手資料」。
2. 選單「擴充功能 → Apps Script」。
3. 把預設的 `程式碼.gs` 內容清空，貼上本專案的 **`LineBot.gs`** 完整內容（工作台「系統設定 → LINE 小幫手」可下載）。
4. 存檔後，在編輯器上方函式選單選 **`setupLineBot`**，按「執行」並完成 Google 授權。
5. 打開「執行紀錄」，會看到初始化摘要，內含：
   - **Webhook 驗證參數**（`?hook=` 後面那串亂數）
   - **工作台同步金鑰**
6. 左側「專案設定（齒輪）→ 指令碼屬性」，新增：
   - `LINE_CHANNEL_ACCESS_TOKEN`：第 1 步的 Channel access token（必填）
   - `GEMINI_API_KEY`：Gemini 金鑰（選填；要用自然語言分類、語音、照片辨識才需要，可到 [Google AI Studio](https://aistudio.google.com/app/apikey) 免費申請）
   - `LINE_ALLOWED_USER_IDS`：先留空，第 4 步再回來填
7. 右上「部署 → 新增部署 → 網頁應用程式」：
   - 執行身分：**我**
   - 存取權：**任何人**（安全由亂數金鑰把關；請勿把網址與金鑰公開）
   - 按「部署」，複製產生的 **`/exec` 網址**。

> 之後若修改程式，要用「部署 → 管理部署 → 編輯 → 版本：新版本」更新，網址不變。

## 第 3 步：設定 Webhook

1. 回到 LINE Developers 的 Messaging API 分頁。
2. **Webhook URL** 填：`<你的 /exec 網址>?hook=<Webhook 驗證參數>`
   例如：`https://script.google.com/macros/s/AKfy…/exec?hook=a1b2c3…`
3. 開啟「Use webhook」，按「Verify」應顯示成功。

## 第 4 步：鎖定只回應你本人（強烈建議）

1. 在 LINE 對你的機器人傳「**我的ID**」，它會回覆你的 userId（一串 U 開頭的英數字）。
2. 回 Apps Script「指令碼屬性」，把它填入 `LINE_ALLOWED_USER_IDS`。
3. 之後陌生人加你的機器人好友，機器人一律不回應、不記錄。

## 第 5 步：連接教師工作台

1. 開啟教師工作台 → 「⚙ 設定 → LINE 小幫手」。
2. 勾選「啟用 LINE 小幫手同步」，貼上 `/exec` 網址與同步金鑰。
3. 按「測試連線」→ 成功後按「立即同步」。
4. 教師工具箱會出現「LINE 收件匣」按鈕；桌面小綿助開啟時也會顯示「LINE 待整理」。

## 第 6 步（選用，強烈建議）：一鍵設定全部自動觸發器

在 Apps Script 編輯器上方函式選單選 **`setupTriggers`** → 按「執行」一次，即自動建立：

| 觸發器 | 時間 | 做什麼 |
| --- | --- | --- |
| 每日簡報 | 每天 8–9 點 | LINE 推播：今日點名回報＋作業繳交＋今日任務＋逾期任務清單 |
| 未點名提醒 | 8:20 起智慧檢查 | 只在當天大屏還沒回報時提醒（備援；太早醒來會自動延後 20 分鐘再查，不誤報） |

> 📌 點名資訊的**主通道**是大屏 **8:08 自動送出**——那一刻 LINE 就會收到完整晨間回報，精確到分。上述雲端觸發器是彙總與備援（Google 觸發器只能保證落在整點區間，無法指定確切分鐘，故以大屏端計時為準）。
| 每週家長關懷 | 每週五下午 3–4 點 | 缺交達門檻（預設 5 次）的委婉家長訊息草稿進工作台收件匣 |

重複執行會重建、不會疊加；之後永久自動，不必再設定。

## 第 6.5 步（選用）：教室電腦開機自動開大屏

1. 把 `啟動大屏kiosk.bat` 複製到教室電腦，用記事本打開，把網址改成你的正式網址（上線後為 `https://…netlify.app/Morning.html`）。
2. 按 **Win+R** 輸入 `shell:startup` 按 Enter，把該檔案放進開啟的「啟動」資料夾。
3. 之後開機自動全螢幕進入大屏，零操作。

---

## 第 7 步（選用）：晨間大屏（點名＋作業繳交）

`Morning.html` 是教室觸控大屏頁，與 LINE 小幫手共用同一份 Apps Script：

1. 在教室電腦（接投影機或觸控大屏）開啟部署網站的 `Morning.html`。
2. 進「⚙ 設定」：填班級名稱、人數、科目清單、`/exec` 網址，以及 `setupLineBot` 執行紀錄裡的**教室大屏金鑰**（CLASSROOM_TOKEN）。
   - 教室金鑰權限只限「回報今天」；請勿把老師同步金鑰存在教室電腦。
3. 晨間流程：
   - 「🙋 點名」：預設全班到校，點座號循環切換 病假→事假→遲到→未到。
   - 「📚 作業繳交」：點科目啟用「今日要收」→ 收件模式點**已交**座號變綠；訂正／補交模式把紅色（缺交）點成藍色（補交）。右側即時顯示缺交名單與全班總表。
   - 按「📤 送出回報」→ 寫入試算表並**立即推播到老師 LINE**；同一天重送會覆蓋。
4. 「📊 查詢統計」：輸入老師同步金鑰＋日期區間，可看出缺勤天數累計、各科缺交／補交天數（分科）、科目繳交率與單日明細。
5. 老師在 LINE 隨時輸入「**今日點名**」「**缺交統計**」也能查詢。
6. 工作台日曆會在同步後顯示每天的出缺與缺交摘要，點徽章看座號明細。

大屏只使用座號，不出現學生姓名；頂部附大電子鐘與倒數計時（5／10 分或自訂），方便晨間常規限時挑戰。

### 大屏 v2 功能重點

- **空號**：設定可填沒有人的座號，不出現在任何格子與統計。
- **點名預設全部未填**；設定裡有三個時間（預設 08:00／08:05／08:08）：
  - 遲到界線後點「到校」→ 大屏顯示「到」，資料記遲到（**遲到只有老師在 LINE 與查詢看得到**）。
  - 自動缺席時間一到，未填座號自動轉「未到」。
  - 自動回報時間一到，本頁自動送出並推播 LINE（教室電腦需開著本頁；中午後開頁不會補做）。
- **科目管理**：內建國小常見科目＋通知單，可自訂新增、勾選哪些出現在作業繳交；作業名稱選單（習作／試卷／作業本／學習單…）也可自訂。
- **收件版面**：今日最多 6 個「科目＋作業名稱」版面同屏並排，各自登記，底部有分科今日統計。
- **作業補交分頁**：追蹤近 30 天（可調）所有缺交，三種檢視（按科目／依座號／日曆）；學生補交後按「完成」即回寫雲端為補交。
- **雲端備援**（選用）：在 Apps Script「觸發條件」新增 `sendRollcallReminder` 每日 8–9 點觸發器；當天沒回報時 LINE 提醒老師。

> ⚠️ **從 v1 升級的老師**：更新 `LineBot.gs` 程式後，務必「部署 → 管理部署 → ✏️ 編輯 → 版本選『新版本』→ 部署」；試算表的「作業紀錄」會自動加上「作業名稱」欄，舊資料保留。

## 使用方式速查

| 你在 LINE 做什麼 | 小幫手回應 |
| --- | --- |
| 直接打「明天要收回條」 | 整理成任務草稿進收件匣 |
| 「記：小明今天撞到頭已冰敷」 | 整理成記事（自動加分類標籤） |
| 傳語音 | 自動聽寫再整理（需 Gemini 金鑰） |
| 拍通知單、黑板 | 辨識重點再整理（需 Gemini 金鑰） |
| 「今天上到哪」「今日任務」 | 回報工作台快照 |
| 「聯絡簿：數習p20、帶直笛」 | 產生可轉發家長群的公告 |
| 「聯絡本：數習P.20、帶直笛」（文字或語音） | **直接寫入今天的聯絡本**，大屏黑板一分鐘內自動顯示 |
| 「撤回」 | 作廢剛剛那一筆 |
| 「刪除 回條」 | 作廢含「回條」的最新一筆 |
| 「說明」 | 顯示使用說明 |

## 費用與額度

- 你傳訊息、機器人回覆：**免費、不限量**。
- 機器人主動推播（晨間簡報）：計入 LINE 免費額度（目前每月 200 則；每日 1 則約用 30 則）。
- Gemini：Google 提供部分模型免費用量，依當時配額與條款。

## 安全與隱私

- Channel token、Gemini 金鑰、同步金鑰只存在你自己的 Apps Script「指令碼屬性」；工作台端的網址與金鑰不會寫入 JSON 備份或 GitHub。
- 試算表只是「轉運信箱」：訊息被工作台收下後，正本保存在你瀏覽器的 localStorage。
- 聯絡簿公告轉發家長群前，請自行確認內容沒有學生姓名等個資。
- 請勿把 `/exec` 網址（含 hook 參數）與金鑰貼給他人或公開張貼。
