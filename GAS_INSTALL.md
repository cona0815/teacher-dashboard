# 教師工作台 GAS 雲端版安裝

完成下列設定後，任務、備份與 Drive 資料庫才會由 Google Apps Script 與 Google 試算表處理。若直接開啟本機 `Index.html` 或 Netlify 網址，系統仍使用該瀏覽器的本機儲存空間。

## 必要檔案

- `Code.gs`：後端、試算表、Google Drive 與權限處理。
- `Index.html`：教師工作台主畫面。
- `Board.html`：電子紙看板。
- `Installer.html`：試算表安裝精靈。

## 安裝步驟

1. 到 Google Drive 建立一份新的 Google 試算表，名稱可填「教師工作台資料庫」。
2. 開啟該試算表，選擇「擴充功能」→「Apps Script」。
3. 在 Apps Script 左側選擇預設的指令碼，將檔名改為 `Code`，清空原內容，再貼入本專案 `Code.gs` 的完整內容。
4. 在 Apps Script 左側按「＋」→「HTML」，建立 `Index`，並貼入 `Index.html` 的完整內容。
5. 用相同方法建立 `Board` 與 `Installer`，分別貼入 `Board.html`、`Installer.html` 的完整內容。
6. 按「儲存專案」。回到 Google 試算表並重新整理頁面。
7. 試算表上方會出現「校務任務系統」選單。選擇「安裝教師工作台」，依安裝精靈完成資料表初始化。
8. 回 Apps Script，選擇右上角「部署」→「新增部署」。
9. 類型選「網頁應用程式」；執行身分選「我」；存取權請依學校 Google Workspace 政策設定。
10. 按「部署」，依 Google 畫面完成授權。
11. 複製部署完成後產生、結尾為 `/exec` 的網址。
12. 之後從該 `/exec` 網址開啟教師工作台。

## 讓小綿助連線

1. 小綿助是 Windows 桌面程式，無法使用瀏覽器內的 Google 登入 Cookie。建議建立第二個「桌面同步專用」部署，存取權允許桌面程式存取（通常是「任何人」）；原本給老師使用的管理台部署仍維持限制登入。若學校 Workspace 禁止公開的 API 部署，小綿助仍可使用本機模式與手動 JSON 匯出／匯入。
2. 因為可由桌面程式存取，務必設定同步金鑰。回到 Google 試算表，選擇「校務任務系統」→「設定小綿助同步金鑰」。
3. 輸入至少 16 個字元的自訂金鑰，或留空讓系統自動產生；妥善保存顯示的結果。
4. 右鍵點小綿助，開啟秘書首頁。
5. 在「雲端連動（選填）」填入「桌面同步專用部署」的 GAS `/exec` 網址與同步金鑰，勾選「啟用 GAS 同步」。
6. 先按「測試連線」，成功後再按「立即同步」。
7. 如果連線失敗，小綿助會保留所有本機資料，不會清空或覆蓋本機檔案。

設定同步金鑰後，匿名使用者即使打開桌面同步專用 `/exec` 網址，也無法讀取管理台資料；桌面端只能透過 POST 並提供正確同步金鑰。請勿把同步金鑰提交到 GitHub、貼在公開網頁或傳給其他人。

## 更新版本

更新 `Code.gs` 或 HTML 檔後，請重新儲存，再到「部署」→「管理部署作業」→「編輯」，建立新版本後部署。原本的 `/exec` 網址可以保持不變。

## 如何確認連線成功

1. 從 `/exec` 網址開啟系統，而不是從電腦上的 `file:///.../Index.html` 開啟。
2. 進入「系統設定」→「備份與還原」。
3. 按「立即雲端備份」。
4. Google Drive 應出現系統建立的備份資料夾與 JSON 備份檔。

## 常見問題

- 看不到「校務任務系統」選單：確認 Apps Script 是從該試算表的「擴充功能」開啟，儲存後重新整理試算表。
- 顯示找不到 HTML 檔：確認 Apps Script 內的檔名正確為 `Index`、`Board`、`Installer`，不要在名稱欄再輸入 `.html`。
- 修改後網頁沒有更新：到「管理部署作業」建立新版本並重新部署。
- 本機測試有資料、GAS 版卻沒有：本機版與 GAS 版是不同儲存來源，請先匯出 JSON，再於 GAS 版還原。
